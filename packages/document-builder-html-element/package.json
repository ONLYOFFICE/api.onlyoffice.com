{
  "name": "@onlyoffice/document-builder-html-element",
  "type": "module",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "clean": "rimraf node_modules",
    "build:code": "esbuild --outdir=dist --tsconfig=tsconfig.emit.json --sourcemap lib/*",
    "build:types": "tsc --project tsconfig.emit.json",
    "build": "pnpm build:types && pnpm build:code",
    "test:types": "tsc",
    "test": "pnpm test:types"
  },
  "peerDependencies": {
    "preact": "10.22.0"
  },
  "dependencies": {
    "@onlyoffice/document-editor-html-element": "workspace:^",
    "@onlyoffice/document-server-types": "workspace:^",
    "@types/web": "0.0.187"
  },
  "devDependencies": {
    "preact": "10.22.0",
    "rimraf": "6.0.1",
    "typescript": "5.4.5"
  }
}
