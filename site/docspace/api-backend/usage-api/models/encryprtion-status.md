# EncryprtionStatus

Possible values:

- `0` - Decrypted (`Decrypted`)
- `1` - Encryption started (`EncryptionStarted`)
- `2` - Encrypted (`Encrypted`)
- `3` - Decryption started (`DecryptionStarted`)
