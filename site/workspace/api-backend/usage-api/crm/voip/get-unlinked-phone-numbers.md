---
title: "Get unlinked phone numbers"
sidebar_label: "Get unlinked phone numbers"
sidebar_class_name: api-method get
description: "Returns a list of all the unlinked phone numbers."
---

**GET** `/api/2.0/crm/voip/numbers/unlinked`

Returns a list of all the unlinked phone numbers.

## Returns

List of unlinked phone numbers. A request that is not authenticated returns `401`.

## Response example

```json
[]
```
